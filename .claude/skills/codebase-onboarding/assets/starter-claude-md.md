# CLAUDE.md

## Commands

```bash
[dev command]      # Start dev server
[build command]    # Production build
[lint command]     # Lint
[test command]     # Run all tests
[coverage command] # With coverage report (if configured)
```

[Note any required env vars or setup steps here, or "No environment variables required."]

## Architecture

[2–4 sentences describing the top-level structure:
which files/dirs own what responsibility, and how data flows between them.]

## Testing

[Testing stack, e.g. "Jest + jsdom + @testing-library/react".]
[Any mocking libraries available, e.g. MSW for API mocking.]

Test files live alongside source files (e.g. `[example path]`).

## Conventions

- **[Convention 1]**: [description]
- **[Convention 2]**: [description]
- **[Convention 3]**: [description]
